local player = game.Players.LocalPlayer
local Maps = workspace:WaitForChild("Maps")

local tycoon -- vamos guardar o tycoon correto aqui

-- Procura o Tycoon do jogador
for _, model in ipairs(Maps:GetChildren()) do
	if model:IsA("Model") and model:FindFirstChild("TycoonOwner") then
		if model.TycoonOwner.Value == player.Name then
			tycoon = model
			break
		end
	end
end

if not tycoon then
	warn("Tycoon do jogador não encontrado!")
	return
end

local placedBoxes = tycoon:WaitForChild("PlacedBoxes")

-- Se quiser pegar TODOS os objetos dentro da pasta:
for _, obj in ipairs(placedBoxes:GetChildren()) do
	print("Objeto encontrado:", obj.Name)
end
